#include "main.h"
/**
 * _islower - prints the content of the code
 *
 * @c: variable to check
 *
 * Descriptions: checks if the variable is lower
 * Return: 1 if c is lower else 0
 *
 */
int _islower(int c)
{
	return (c >= 97 && c <= 122);
}
